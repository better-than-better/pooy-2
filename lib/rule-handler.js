module.exports = function ruleHandler(rule) {
  const { testField } = rule;

  if (testField === 'url') {
    return;
  }

  if (testField === 'requestHeader') {

  }

  if (testField === 'responseHeader') {
    
  }
};
